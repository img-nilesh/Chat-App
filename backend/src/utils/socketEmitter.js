// const { getIO } = require("../config/socket");

// const emitBookingRequest = (ownerId, data) => {
//     const io = getIO();
//     io.to(ownerId).emit("booking_request", data);
// };

// module.exports = { emitBookingRequest };